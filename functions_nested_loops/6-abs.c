#include "main.h"
/**
 * _abs - check if a char is lowercase
 * @c: The character to check
 *
 * Return: On success 1.
 * On error, 0 is returned.
 */
int _abs(int c)
{
int a;
if (c > -1)
{
return ("%d", c);
}
else
{
a = c * -1;
return ("%d", a);
}
}
